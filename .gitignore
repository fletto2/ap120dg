dgasm/build/
*.o
*.bin
__pycache__/
rsx/*.gz
rsx/*.dsk
rsx/*.rk
rsx/console.log

# build products extracted from the RSX transfer volume
*.OBJ
*.TSK
*.OLB
*.MAP
HASI

# Built RSX volumes: reproducible from rsx/fps_faithful_install.cmd,
# far too large for git (the RK07 alone is 55 MB).
rsx/fps.installed/

# Scratch transfer volumes built by rsx/asm_obj.sh and rsx/refjob.sh
# (35 MB each, regenerated on every run)
asmobj-work/
refjob-work/
rsx/*.log
